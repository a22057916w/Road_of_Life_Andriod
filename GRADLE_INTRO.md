# 🐘 Gradle : From Build to Deploy 
## Gradle Directories
Gradle 主要使用兩個目錄來運作，分別為 **Gradle User Home Directory** 及 **Project Root Dierctory**。
![image](https://hackmd.io/_uploads/SkpNIZmqee.png)
### Gradle User Home Directory
Gradle User Home 通常位在 `~/.gradle` 或 `C:\Users\<USERNAME>\.gradle`，並儲存相關建置檔如 `properties`, `initialization scripts`, `caches`, 和 `log`。

大致結構如下:

```text
├── caches  ①
│   ├── 4.8 ②
│   ├── 4.9 ②
│   ├── ⋮
│   ├── jars-3  ③
│   └── modules-2   ③
├── daemon ④
│   ├── ⋮
│   ├── 4.8
│   └── 4.9
├── init.d   ⑤
│   └── my-setup.gradle
├── jdks     ⑥
│   ├── ⋮
│   └── jdk-14.0.2+12
├── wrapper
│   └── dists   ⑦
│       ├── ⋮
│       ├── gradle-4.8-bin
│       ├── gradle-4.9-all
│       └── gradle-4.9-bin
└── gradle.properties   ⑧
```

① Global cache directory 
- 全域快取資料夾（不是專案獨有的東西都放這裡）.

② Version-specific caches (e.g., to support incremental builds).
- 不同 Gradle 版本快取，以支援 [incremental builds](https://docs.gradle.org/current/userguide/incremental_build.html)。

③ Shared caches
- 共用快取，比如下載過的第三方 library（dependencies）。
- 好處：不同專案用到同一個 library，不用每次都重編或重下。

④ Registry and logs of the Gradle Daemon.
- [Gradle Daemon](https://docs.gradle.org/current/userguide/gradle_daemon.html#gradle_daemon)（常駐背景程式）的登錄資訊與執行紀錄。
- 它會長駐記憶體，讓你多次 build 時更快。

⑤ Global [initialization scripts](https://docs.gradle.org/current/userguide/init_scripts.html#init_scripts).
- 全域的初始化腳本（在所有專案跑之前會先執行的設定檔）。

⑥ JDKs downloaded by the toolchain support.
- 如果你用了 Gradle 的 [Java toolchain](https://docs.gradle.org/current/userguide/toolchains.html#sec:provisioning) 功能，它會自動幫你下載對應版本的 JDK，放在這裡。

⑦ Distributions downloaded by the [Gradle Wrapper](https://docs.gradle.org/current/userguide/gradle_wrapper.html#gradle_wrapper_reference).
- 透過 wrapper (gradlew) 自動下載的 Gradle 本體（各版本），都存放在這裡。

⑧ Global Gradle [configuration properties](https://docs.gradle.org/current/userguide/build_environment.html#sec:gradle_configuration_properties).
- 全域的設定檔，例如 ~/.gradle/gradle.properties。

### Project Root directory
此目錄包含了專案的所有原始檔案，以及 Gradle 自動生成的目錄與檔案，例如 `.gradle`、`build` 與 `gradle` 設定目錄。

其中：
- `gradle` 目錄通常會被提交版本控制，
- 而 `build` 與 `.gradle` 目錄則存放建構輸出（build）、快取與 Gradle 用於支援增量建置（incremental builds）的暫存檔案。

專案根目錄的典型結構如下：
```text!
├── .gradle ①
│   ├── 4.8 ②
│   ├── 4.9 ②
│   └── ⋮
├── build   ③
├── gradle
│   └── wrapper ④
├── gradle.properties   ⑤ 
├── gradlew ⑥
├── gradlew.bat ⑥
├── settings.gradle.kts ⑦
├── subproject-one  ⑧
|   └── build.gradle.kts    ⑨
├── subproject-two  ⑧
|   └── build.gradle.kts    ⑨
└── ⋮
```

① Project-specific cache directory generated by Gradle. 
- 專案特定的快取目錄，由 Gradle 自動建立  

② Version-specific caches (e.g., to support incremental builds).
- 版本特定的快取（增量建置使用）

③ The build directory of this project into which Gradle generates all build artifacts.
- 專案的 build 輸出目錄，儲存所有建構結果  

④ Contains the JAR file and configuration of the.
- Gradle Wrapper 的 JAR 與設定  

⑤ Project-specific Gradle configuration properties.
- 專案特定的 Gradle 設定屬性  

⑥ Scripts for executing builds using the Gradle Wrapper.
- Gradle Wrapper 執行腳本（Unix 與 Windows）  

⑦ The project’s settings file where the list of subprojects is defined.
- 定義子專案清單的設定檔  

⑧ Usually, a project is organized into one or multiple subprojects.
- 子專案的結構（常見的專案組織方式）  

⑨ Each subproject has its own Gradle build script.
- 每個子專案擁有自己的 Gradle 建構腳本